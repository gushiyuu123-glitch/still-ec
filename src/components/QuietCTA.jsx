export default function QuietCTA() {
  return (
    <section className="w-full py-32 text-center bg-white">
      <h2 className="text-xl font-light tracking-[0.2em] mb-4">
        STILL — Quiet, Refined, Essential.
      </h2>

      <a
        href="#collection"
        className="text-sm text-gray-600 underline-offset-4 hover:underline"
      >
        Discover the full collection →
      </a>
    </section>
  );
}
